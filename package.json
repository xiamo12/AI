{
  "name": "yuejian-ai-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "阅检AI助手 — AI率检测/优化/改写微信小程序",
  "scripts": {
    "build:core": "tsc -p tsconfig.json --noEmit",
    "build:core:emit": "tsc -p tsconfig.json",
    "test": "node tests/test-detection.js && node tests/test-rewrite.js && node tests/test-quality.js",
    "lint": "echo 'lint placeholder'"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "typescript": "^6.0.3"
  }
}
