# 阅检AI助手 — Claude Code 工作流规则

## 角色加载
你是一个开发 Agent。在执行具体任务前，请按以下方式加载角色：

1. 先读取 `agents/agent-config.md` 了解当前处于哪个阶段
2. 读取对应阶段的 skill 文件（`skills/<role>.md`）
3. 完全按照 skill 文件中定义的"输入"列表读取文档

## 核心原则

1. **不编造文件和函数** — 访问一个文件/函数前，先确认它真实存在
2. **读再写** — 修改前先读取完整文件内容
3. **小步提交** — 每次只改一个逻辑单元
4. **遵守模块边界** — pages/ ← utils/ ← core/ 单向依赖，禁止反向

## 上下文管理

- 每次任务开始时，先确认自己是谁（哪个 Agent、哪个 Phase）
- 执行前加载对应的 skill 文件
- 完成后输出简要总结（改了什么、下个阶段该做什么）

---

## Karpathy 编码准则
*以下规则来自 multica-ai/andrej-karpathy-skills（133k⭐）*

### 1. Think Before Coding — 先想再写
**不要假设。不要隐藏困惑。暴露取舍。**
- 实现前，明确陈述假设。不确定就问。
- 如果存在多种解释，全部列出来，不要偷偷选一个。
- 如果有更简单的方法，就说出来。该推就推。
- 如果某件事不清楚，停下来。说出问题。问。

### 2. Simplicity First — 简单优先
**解决问题的最少代码。不写投机代码。**
- 不做需求之外的功能。
- 不给一次性代码造抽象。
- 不要没被要求的"灵活性"和"可配置性"。
- 不为不可能场景写错误处理。
- 如果你写了 200 行但可以缩成 50 行，重写。

### 3. Surgical Changes — 外科手术式修改
**只碰必须碰的代码。只清理自己制造的垃圾。**
- 不改旁边没问题的代码、注释、格式。
- 不重构没坏的东西。
- 匹配现有代码风格，即使你会用不同方式写。
- 发现无关的死代码，提一句就行，别删。
- 你改完导致的孤儿：删除你的改动造成的未用 import/变量/函数，但别删本来就在的死代码。

### 4. Goal-Driven Execution — 目标驱动执行
**定义成功标准。循环直到验证。**
- "加验证" → "写测试让无效输入通过，再让它通过"
- "修 Bug" → "写复现测试，再让它通过"
- "重构 X" → "确保测试前后通过"
- 多步任务先列计划：
  ```
  1. [Step] → 验证: [check]
  2. [Step] → 验证: [check]
  ```

**这些准则生效的标志：** diff 里不必要的改动更少、因为过度复杂而重写的次数更少、澄清问题在实现之前提出而不是错误之后。
